# Additional dependencies alongside the existing neural environment.
# Runtime tested with Python 3.11 and numpy 1.24.4.
# brian2's legacy setup.py needs numpy, cython and setuptools<81 (pkg_resources
# removed in 81+) already installed, and must be built with build isolation
# disabled for it, e.g.:
#   uv pip install numpy==1.24.4 cython "setuptools<81" wheel
#   uv pip install --no-build-isolation-package brian2 -r doom/requirements.txt
numpy==1.24.4
brian2==2.5.1
vizdoom==1.3.0
numba==0.61.2
pillow==11.3.0
pytest==8.4.2
pyarrow==20.0.0
scipy==1.11.4
pandas==2.1.4
